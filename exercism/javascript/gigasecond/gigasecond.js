export const gigasecond = (date) => {
	const parsed = Date.parse(date);
	console.log(parsed);
	// add gigasecond (1000000000000) -> in milliseconds, so there's no conversion to seconds
	// convert back to date
};
