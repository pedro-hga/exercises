function isAnagram(s: string, t: string): boolean {
  // check if s and t are the same size
  if (s.length !== t.length) false;

  // create empty hash
  interface IHash {
  }
  let sChars: IHash = {};
  let tChars: IHash = {};
  // traverse through the first one and save each letter to a hash with a counter
  // check to see if its already there, if not, set it to one, otherwise, add to it

  // do the same for the other string and compare both hashes

};
